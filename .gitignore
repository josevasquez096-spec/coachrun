node_modules
.next
.env.local

# generados por Next y TypeScript al compilar
next-env.d.ts
*.tsbuildinfo

# la compilación estática para el APK (se genera con scripts/compilar-movil.sh)
out
movil/www
